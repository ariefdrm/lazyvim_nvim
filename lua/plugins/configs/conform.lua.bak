return {
  require("conform").setup({

    formatters_by_ft = {
      blade = { "blade-formatter" },
    },

    format_after_save = {
      timeout_ms = 100,
      lsp_format = "prefer",
    },

    default_format_opts = {
      quiet = false,
      timeout_ms = 100,
      lsp_format = "fallback",
    },
  }),
}
